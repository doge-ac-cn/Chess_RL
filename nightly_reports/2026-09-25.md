# 2026-09-25 夜报（23:00-05:50）

## 五子棋（gomoku/）
- V3（6×96，1.12M）续训：iter 27 → 67（+40 轮），2 次晋级（gate 0.667×2）。
- 对抗评估（各 100 局，sims 128）：V3 vs V2 = 50/50（两晚结构相同），
  V3 vs 老师 = 78/100，执黑 50/50 全胜保持。
- **决策：未达 55% 替换线，App 继续使用 V2 模型**。
- 结论：当前自弈数据量下，模型规模翻倍未带来强度增益——瓶颈在自弈对局数
  （每轮 ~20 局 × sims 96 太少）。下一步优先提高自弈吞吐（多进程已就绪，
  需要更多机器时间），或保持 V2 每晚续训。

## 象棋（xiangqi/）
- RL 大规模续训：昨晚 8 轮 + 今夜 40 轮（sims24）+ 24 轮（sims48，从 rl_v2
  断点续训）= 累计 72 轮自弈，rl_v3.pt 已存档。
- **如实结论：rl_v3 vs 老师 AI（sims48 × 10 局）= 0W 8L 2D，老师 AI 仍明显
  更强。** 自弈 RL 处于早期"战力低谷"（弱网自弈数据质量有限），40+24 轮的
  训练量远不足以渡过低谷（对照：五子棋用了数千局）。
- 已完成的关键基建：重复局面判和（3 次重复）、MCTS 适配、掩码策略 CE、
  断点续训——规模扩大后随时可继续。
- **决策：网页象棋 AI 继续使用"老师启发式 + 网络微调"组合（当前更强），
  RL 网络继续训练直到强度探针转正再切换。**

## 围棋（go/）
- P1 管线完成：encode（4 平面）/ GoNet（9 路，35 万参数）/ MCTS（含 pass
  与数子终局）/ 自弈 / 训练循环，冒烟通过（9 路完整对局 111/117 手，
  数子合理，loss 下降）。**P1 训练排下一晚**。

## 指标变化
- gomoku V3：价值损失 0.29-0.36，policy 损失 3.9-4.2，执黑对老师 1.0。
- xiangqi rl：loss 5.29（初）→ 2.33-2.46（收敛中），自弈对局在判和规则下
  可自然分出胜负。

## 遇到的问题
- 无新问题。强度探针脚本的一处 numpy 导入遗漏当轮修复。

## 下一步计划
1. gomoku：增加自弈吞吐后继续 V3；或按用户反馈调整方向。
2. xiangqi：多进程自弈提量（复用 gomoku 框架）+ BC 数据扩充 → 目标：
   RL 网络 vs 老师 AI 胜率转正 → 再谈替换网页 AI。
3. go：P1 正式训练（9 路小型网 × 数晚续训）→ P2 评估。
