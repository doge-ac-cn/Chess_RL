# 2026-09-20 夜报（00:58 会话）

## 完成事项

### 五子棋（gomoku/）
- 从 checkpoints_v2/best.pt (iter 42) 断点续训 V2 模型，--hours 3.0 预算
  预计 ~04:00 自行结束（checkpoint 自动落盘）。
- 昨日已完成：V2 训练 43 轮；对抗评估 V2 vs V1 = 50/50（各执黑全胜），
  执黑对老师 50/50；未达替换阈值（≥55%），App 继续使用 V1 模型。

### 象棋（xiangqi/）—— 新增子项目
- BC 热启动管线完成并验证（补充于 01:59）：
  `encode.py`（15 平面：7 类×双方+颜色）、`net.py`（from/to 分解式策略头，
  合法性掩码，1.39M 参数）、`train_bc.py`（老师自弈数据 9092 局面，胜负均衡
  2656W/2636L）。6 轮训练 legal-top1 从 20% 升至 **59.1%**，模型存 bc_net.pt。
  自我对弈 RL 移植留待下一晚（管线已验证，仅需把 gomoku 的 MCTS 适配到
  legal_moves 接口）。
- 规则引擎完成：9×10 棋盘、七兵种走法（马腿/象眼/炮架/过河兵）、
  九宫约束、将帅照面、将军检测、将死/困毙终局、着法生成、undo 可逆。
- 11 项单元测试全部通过，关键校验：开局合法着法 = 44（标准值）。
- 过程中修复 2 个 bug：
  1) `_attacks` 将兵(P)错误路由进炮的滑行攻击逻辑 → 幻影将军；
  2) 测试布局误触发将帅照面（测试问题）。
- 老师 AI（子力+位置 1-ply 贪心，稳定 softmax 温度采样）：执红 2:0 随机对手；
  老师互弈 200 手仅 1.3 秒（~6ms/着），可支撑后续 BC 数据生成。
- 自弈冒烟：随机 vs 随机 300 手无崩溃；老师 vs 老师 200 手完整对局。

### 围棋（go/）—— 新增子项目
- DESIGN.md：P0-P4 分期计划（9路→13路→19路，网络 60万→2500万 参数，
  多晚续训累积，P4 对接 KataGo）。
- 规则引擎完成（9/13/19 路通用）：提子、简单劫、禁自杀、pass 终局、
  中国规则数子（区域法+贴目7.5），5 项测试全部通过。

## 指标变化
- gomoku V2 续训完成：iter 43 → 84（+41 轮），本轮 5 次晋级（第 43/47/59/72/79 轮
  附近），vs_teacher_black 稳定 1.0，价值损失 0.38 → 0.377，policy 损失 3.03 → 3.52
  （自弈目标更难，正常）。best.pt 已更新，下一晚 --resume 继续。
- xiangqi 规则引擎：11/11 测试通过；开局着法数 44 ✓（规则正确性强校验）。
- xiangqi BC：legal-top1 0.20 → 0.59（6 轮），因子分解策略头+合法掩码方案验证成功。

## 遇到的问题
- 无新问题。象棋引擎的两个 bug 在测试驱动下当轮修复。

## 下一步计划
1. gomoku：V2 继续每晚续训；当对 V1 对抗 ≥55% 时替换 App 模型。
2. xiangqi：移植 gomoku 的 encode/net/selfplay/train 框架（动作空间 90×90
   from-to），先跑通小规模 BC+自弈管线，再谈强度与 bigger model。
3. go：9 路小型网络 + MCTS 自弈管线（P1），规则引擎已就绪。


## 补充（07:54 会话）：修复线上 App 崩溃
- 用户实测报告 `Assignment to constant variable`：定位为移动端 ai.js 防叉守卫
  替换着法时对 `const move` 赋值（早前整体重写时回归引入，之前修过一次被覆盖）。
  该 bug 恰好在"守卫决定换防"的最关键时刻触发，导致 AI 该手直接报错。
- 修复：`const { move } = await mcts(...)` → `let move`；新增 Node 回归测试
  `tests/test_ai_js.mjs`（含确定性命中替换路径的 phase3 用例，直接复现原崩溃），
  修复后全绿。SW 缓存版本 v5 → v6，用户需强制刷新两次生效。


## 补充2（13:10 会话）：定式死代码 + SW 缓存地狱修复
- 用户实测再暴露两个问题：
  1) 白棋邻域定式的 restrict 是死代码——算出来后从没传进 MCTS，导致 AI 第二手
     脱离主战场（J10 式远手）；已修复（restrict 传入根节点扩展）。
  2) localhost 开发被 Service Worker 缓存地狱困住（SW 的 ignoreSearch:true 把
     所有破缓存请求都拦回旧文件）；sw.js 加本地自毁守卫，ignoreSearch 改为
     false，SW 升 v7；dev server 换成发 no-store 头的 server.py。
- 浏览器/Python 双端推理逐位一致性已验证（value=-0.6502、top8 完全一致），
  排除模型导出问题；回归测试 PHASE3 确定性命中防叉替换路径，修复后全绿。


# 2026-09-20/21 主夜段（23:00-02:30）

## 五子棋：V2 续训 +43 轮并完成模型换代
- iter 85 → 127（本轮 +43），期间 4 次晋级（含 gate 0.833 两次），价值损失稳定。
- **对抗评估（各 100 局，sims 128）**：
  - V2 vs V1：**58/100（score 0.580 ≥ 55% 替换线）**，执黑 50/50、执白 8/50；
  - V2 vs 老师 AI：72/100，执黑 50/50 全胜。
- **App 模型已替换为 V2**（562k 参数，fp32 2.2MB / int8 582KB，一致性校验通过；
  旧 V1 模型备份为 gomoku_policy_value_v1.onnx）。浏览器实测：加载正常、
  自适应校准 256 sims、白棋应手贴身合理、延迟约 4 秒。

## 象棋：自弈 RL 管线端到端跑通
- 新增 mcts.py（legal_moves 候选 + from/to 因子分解先验）、selfplay.py、
  train_rl.py（掩码策略 CE + 价值 MSE）。
- 小规模验证：BC 热启动 → 8 轮自弈（各 2-6 局，最长 300 手）→ loss 5.29 → 2.47，
  checkpoint rl_v1.pt 已存档。
- 强度探针（vs bc_net，sims16 × 6 局）全部 300 手封顶未分——弱网自弈长将/循环
  问题暴露。
- **已修复**：规则引擎新增重复局面判和（pos_keys 列表记账 + 3 次重复即和，
  12 项测试全过），自弈循环接入后对局可自然分出胜负（142/133 手判负，
  不再 300 手和棋）。
- 多进程数据生成 + 正式规模训练（更长自弈、更大 batch）排下一晚。

## 围棋
- 规则引擎与 DESIGN.md 昨日已完成；P1（9 路自弈管线）排下一晚。

## 下一步计划
1. gomoku：V2 继续每晚续训；探索 6×96/V3 架构与更高质量自弈（sims 128）。
2. xiangqi：加循环判负规则 → 多进程自弈 → 正式 BC+RL 训练 → vs 老师 AI 强度评估。
3. go：P1 自弈管线（9 路小网 + MCTS），复用同一框架。


## 补充2（03:10）：当晚第二阶段
- 象棋：重复判和规则修复完成（12/12 测试），自弈可自然分出胜负（142/133 手）。
- gomoku：第二轮续训完成（iter 127），对抗评估 **V2 vs V1 = 50/50（两晚均 50），
  vs 老师 = 94/100（执黑 50/50）**——当前规模迭代已饱和。
- **按"打不过就加大模型"策略启动 V3**：6 残差块×96 通道（1.12M 参数），
  修复后 BC 起步（19% top-1），3.5h 预算（~06:45 自停），checkpoints_v3/。
  下一晚 --resume 继续；评估 ≥55% vs V2 时替换 App 模型。
- xiangqi RL 后台完成 8 轮（rl_v1.pt），长将/循环导致的和局已由判和规则解决。


## 补充3（06:42）：V3 首夜训练完成
- V3（6×96，1.12M 参数）完成 27 轮（3.5h，BC 300 局热启动）。
- 2 次晋级（第 17/25 轮，后者 gate 0.667），价值损失 0.28（V2 同期 0.36-0.40，
  更强的开局直觉），vs_teacher_black 稳定 1.0。
- 强度评估（vs V2 ≥55% 替换线）按计划留给今晚 23:00 的定时任务执行。


## 补充4（13:50）：三棋网页入口上线
- 入页改为选择页（index.html），五子棋移至 gomoku.html，新增 xiangqi.html、
  go.html 占位（训练中）。
- **象棋网页版上线**：规则引擎 JS 移植（开局 44 步与 Python 引擎逐手一致）+
  BC 策略模型 ONNX 推理（5.4MB）+ 两段式选子交互。
- 过程中又抓到并修复一个移植 bug：legalMoves 的撤回未恢复目标格原内容，
  每次合法性检测都会留下幻影棋子污染棋盘（新增"44 步与 Python 逐手一致 +
  棋盘零突变"双重校验）。SW 升 v9。


## 补充3（23:10）：象棋模型改用 FP16
- int8 动态量化在 ORT-web 缺 ConvInteger 实现（不可用）→ 改用 FP16 转换
  （keep-io types）：5.4MB → 2.7MB，输出一致性校验通过，浏览器对弈验证正常。
