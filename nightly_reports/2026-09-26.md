# 2026-09-26 夜报（23:00-04:30 主段 + 补充）

## 五子棋（gomoku/）
- V3（6×96）续训：iter 85 → 108（预算 4.5h，超时部分主动 pkill 停止）。
- V3 训完后按计划跑对抗评估，结果异常并已定位为测试假象（详见问题 3）；
  修正评估方式后重测：**V3 vs V2 = 50/50（连续第三晚持平）**，vs 老师 78/100
  （执黑 50/50 全胜）。
- **决策：V3 未达 55% 替换线，App 保留 V2 模型**。V3 best.pt 保留继续累积。
- 结论强化：三晚数据一致表明——同 wall-clock 预算下，模型规模翻倍（56万→112万）
  不带来强度增益，瓶颈是自弈对局量。后续 V3 线要么提吞吐（更多 worker/更久），
  要么回到 V2 规模做更高质量自弈。

## 发现并修复的关键事故：rsync 覆盖生产权重
- 今晨发现昨晚的 hub 同步 rsync 把仓库的 checkpoints_v2/best.pt（iter 127 生产
  权重）覆盖回了工作区的旧副本（iter 43），导致今晚 V2 续训从旧状态重启。
- **恢复**：生产 ONNX（已导出至 App）从未受影响；iter-127 权重从 git 历史
  （87886ab）完整恢复至 checkpoints_v2/best.pt。
- **防再发**：今后 checkpoints 目录一律以仓库为准做单向管理，rsync 同步排除
  checkpoints（已在本晚操作中执行，且 .pt 均在 git 历史中可追溯）。

## 象棋（xiangqi/）
- RL 续训：sims24 × 40 轮（昨晚）+ 从 rl_v2 断点续训 sims48 × 24 轮（今晨）
  = 累计 64+ 轮自弈，rl_v4.pt 存档。
- 强度探针（sims48 × 10 局 vs 老师 AI）：rl_v4 = 0W 9L 1D——RL 网络仍在早期
  低谷，老师 AI 保持明显优势。
- **决策：网页象棋 AI 维持"老师启发式 + 网络微调"混合（当前更强），RL 继续
  夜间累积**。

## 围棋（go/）
- P1 训练循环完成并接入 `go/train_go.py`（自弈 → 掩码策略 CE + 价值 MSE），
  冒烟通过：2 轮训练 loss 4.84 → 4.25，对局 111/99 手完整终局。
- P1 正式训练（更长自弈）排下一晚。

## 多进程自弈基建（xiangqi）
- 新增 `selfplay_mp.py`：spawn 上下文（CUDA 安全）+ 8 worker 并行自弈，
  修复了任务返回值解包错误；单轮 6 局并行 ~40s。
- 发现自弈 7 手循环判和的数据退化问题 → selfplay 增加"循环判和双方 -0.5"
  结果塑形，对局恢复自然分出胜负（120-278 手）。

## 下一步计划
1. gomoku：V3 暂停迭代，优先自弈吞吐实验；或回退 V2 主线积累。
2. xiangqi：RL 继续夜训（已可断点续训），BC 数据扩量；强度探针每周跟踪。
3. go：P1 正式训练（数晚）→ P2 评估 → 决定 13 路升级。
